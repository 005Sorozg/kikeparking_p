// se guardan los colores de toda la app en un solo lugar.
export const colors = {
  background: "#FAFAFA",     // fondo general de las pantallas
  surface: "#FFFFFF",        // fondo de tarjetas y formularios
  primary: "#E63946",        // el rojo principal (botones, ícono, acentos)
  text: "#1A1A1A",           // texto principal, 
  textLight: "#767676",      // texto secundario, 
  border: "#E0E0E0",         // bordes de inputs y tarjetas
  white: "#FFFFFF",
};


export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

export const radius = {
  sm: 8,
  md: 12,
  lg: 24,
};